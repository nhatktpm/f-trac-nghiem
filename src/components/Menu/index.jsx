import React from 'react';
import PropTypes from 'prop-types';
import "./styles.css"

Menu.propTypes = {
    
};

function Menu(props) {
    return (
        <div className="menu">
            Menu
        </div>
    );
}

export default Menu;